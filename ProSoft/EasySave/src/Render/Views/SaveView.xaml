<UserControl x:Class="EasySave.src.Render.Views.SaveDeleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resources="clr-namespace:EasySave.Properties"
             xmlns:local="clr-namespace:EasySave.src.Render.Views"
             xmlns:navigation="clr-namespace:EasySave.src.Render.Views"
             xmlns:easySave="clr-namespace:EasySave"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="650">
    
    <UserControl.Style>
        <Style>
            <Style.Triggers>
                <EventTrigger RoutedEvent="UserControl.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0.0"
                                             To="1.0"
                                             Duration="0:0:0.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    
    <Grid VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Height="25" Width="150" Content="{x:Static resources:Resource.Load}"/>
        <Button Grid.Column="1" Height="25" Width="150" Content="{x:Static resources:Resource.Resume}"/>
        <Button Grid.Column="2" Height="25" Width="150" Content="{x:Static resources:Resource.Cancel}"/>
        <Button Grid.Column="3" Height="25" Width="150" Content="{x:Static resources:Resource.Delete}"/>
    </Grid>
    
    <!--
    <UserControl.Style>
        <Style>
            <Style.Triggers>
                <EventTrigger RoutedEvent="UserControl.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0.0"
                                             To="1.0"
                                             Duration="0:0:0.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid Background="#303030">
        <Grid.RowDefinitions>
            <RowDefinition Height="81*"/>
            <RowDefinition Height="169*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static resources:Resource.HomeMenu_Delete}"
                   FontSize="24"
                   FontFamily="/Fonts/OpenSans-Regular.ttf #Open Sans Regular"
                   Foreground="#DDDDDD"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="5,12,0,0" />

        <ListBox x:Name="DeleteListBox" Margin="10,58,10,250" Grid.RowSpan="2" SelectionMode="multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
            <Button x:Name="DeleteButton" 
                Content="{x:Static resources:Resource.Delete}" 
                HorizontalAlignment="Left" 
                Margin="422,115,0,0" 
                VerticalAlignment="Top" 
                Width="200" Click="DeleteButton_Click" Grid.Row="1"/>
    </Grid>
-->
</UserControl>

